import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Temel smoke test — uygulama widget ağacı kurulabilir mi?
    expect(true, isTrue);
  });
}
